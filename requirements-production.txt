# Add python-decouple for environment variable management (production)
python-decouple==3.8

# Add psycopg2-binary for PostgreSQL support
psycopg2-binary==2.9.9

# Add gunicorn for production WSGI server
gunicorn==21.2.0
